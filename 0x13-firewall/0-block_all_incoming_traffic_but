# update the system
sudo apt update
sudo apt upgrade -y

# install ufw if not already installed
sudo apt install ufw

# block all incoming traffic
sudo ufw default deny incoming

# allow port 22, 443, and 80
sudo ufw allow 22
sudo ufw allow 443
sudo ufw allow 80

